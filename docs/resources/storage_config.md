---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "registry_storage_config Resource - registry"
subcategory: ""
description: |-
  Manages a registry storage backend configuration (local, S3, Azure Blob, GCS).
---

# registry_storage_config (Resource)

Manages a registry storage backend configuration (local, S3, Azure Blob, GCS).

## Example Usage

```terraform
resource "registry_storage_config" "s3" {
  backend  = "s3"
  activate = true
  config = {
    bucket     = "my-registry-bucket"
    region     = "us-east-1"
    access_key = var.aws_access_key
    secret_key = var.aws_secret_key
  }
}

variable "aws_access_key" {
  type      = string
  sensitive = true
}

variable "aws_secret_key" {
  type      = string
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `backend` (String) Storage backend type: 'local', 's3', 'azure', or 'gcs'.
- `config` (Map of String, Sensitive) Backend-specific configuration key-value pairs (e.g., bucket name, credentials). All values are stored encrypted.

### Optional

- `activate` (Boolean) Set to true to make this the active storage backend after creation.

### Read-Only

- `active` (Boolean) Whether this is the currently active storage backend.
- `created_at` (String) ISO 8601 timestamp when the config was created.
- `id` (String) UUID of the storage configuration.
- `updated_at` (String) ISO 8601 timestamp when the config was last updated.
